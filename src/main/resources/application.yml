###########################################################################
# 🎯 어떤 환경을 기본으로 실행할지 결정 (local / prod)
# - 기본은 'local'로 실행되도록 함
# - 운영 시: VM옵션에  -Dspring.profiles.active=prod  지정
###########################################################################
spring:
  profiles:
    active: local   # ✅ 기본 환경: 로컬(집)

  #########################################################################
  # ✅ 애플리케이션 정보
  #########################################################################
  application:
    name: MyBaseLinkV2

  #########################################################################
  # ✅ UI 템플릿 엔진(Thymeleaf)
  #########################################################################
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    mode: HTML
    encoding: UTF-8

  #########################################################################
  # ✅ 정적 리소스 제공 경로
  #########################################################################
  web:
    resources:
      static-locations: classpath:/static/,classpath:/public/,classpath:/resources/

  #########################################################################
  # ✅ 파일 업로드 설정
  #########################################################################
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

  #########################################################################
  # ✅ DevTools 자동 리로드 설정
  #########################################################################
  devtools:
    restart:
      enabled: true
      poll-interval: 1000
      quiet-period: 500
    livereload:
      enabled: true

###########################################################################
# 🖥 서버 공통 설정
###########################################################################
server:
  port: 8080
  servlet:
    context-path: /
  error:
    whitelabel:
      enabled: false

###########################################################################
# 🔐 JWT 토큰 설정 (공통) - 토큰 설정과 시큐리티 설정은 분리해야 관리가 쉽다
###########################################################################
jwt:
  secret: MyNovaSecretKeyChangeThisToSomethingStrong123!
  expiration: 3600000

###########################################################################
# 🔐 Security (JWT) 예외 경로 설정
###########################################################################
security:
  jwt:
    ignore-paths:
      - /api/stock/lastCloseDownward/chart/**
      - /api/stock/lastCloseDownward/sse
      - /api/stock/batch/sse
      - /api/stock/similar-advanced/chart/**
      - /api/stock/similar-advanced/sse

###########################################################################
# 🧠 로깅 레벨 (공통)
###########################################################################
logging:
  level:
    '[org.hibernate.SQL]': ERROR
    '[org.hibernate.type.descriptor.sql.BasicBinder]': ERROR
    '[com.mybaselink]': DEBUG
    '[org.springframework]': INFO
    '[org.apache.catalina.connector.CoyoteAdapter]': ERROR
    '[org.apache.coyote.http11.Http11Processor]': ERROR
    '[org.apache.catalina.connector.OutputBuffer]': ERROR
